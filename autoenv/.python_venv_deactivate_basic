# Basic python venv - deactivate

BASE_PATH="${HOME}"
PWD=`pwd`

if [[ "${BASE_PATH}" == "${PWD}" ]]; then
  # Hide error message is not activated
  declare -f -F deactivate &>/dev/null

  if [[ "${?}" == "0" ]]; then
    deactivate
  fi
fi

